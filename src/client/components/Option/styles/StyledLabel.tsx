import styled from 'styled-components';

const StyledLabel = styled.label`
  cursor: pointer;
  font-family: Manrope;
  font-size: 14px;
  font-weight: 400;
  line-height: 22px;
`;

export default StyledLabel;
