import styled from 'styled-components';
import { Col } from 'react-bootstrap';

const StyledOptionWrapper = styled(Col)`
  display: flex;
  justify-content: center;
  margin-bottom: 0.5rem;
`;

export default StyledOptionWrapper;
