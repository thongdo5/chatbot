import { Alert } from 'react-bootstrap';
import styled from 'styled-components';

const StyledNoticeAlert = styled(Alert)`
  border: none;
  background-color: unset;
  text-align: center;
  color: #842029;
`;

export default StyledNoticeAlert;
