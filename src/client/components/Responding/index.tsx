import React, { FC } from 'react';
import RespondingDots from '@assets/responding-dots.png';

const Responding: FC = () => {
  return <img src={RespondingDots} alt='loading' />;
};

export default Responding;
