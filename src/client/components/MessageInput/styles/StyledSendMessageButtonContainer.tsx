import { Col } from 'react-bootstrap';
import styled from 'styled-components';

const StyledSendMessageContainer = styled(Col)`
  display: flex;
  justify-content: center;
  align-items: center;
`;

export default StyledSendMessageContainer;
