import styled from 'styled-components';

const StyledAvatarTitle = styled.p`
  font-family: Manrope;
  font-size: 1rem;
  font-weight: 400;
  line-height: 1.5rem;
  letter-spacing: 0em;
  color: #ffffff;
`;

export default StyledAvatarTitle;
