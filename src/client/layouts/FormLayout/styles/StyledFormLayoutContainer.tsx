import styled from 'styled-components';

const StyledFormLayoutContainer = styled.div`
  display: flex;
  align-items: center;
  padding: 0 1.75rem;
  height: 100vh;
`;

export default StyledFormLayoutContainer;
